name: carbon_mrv
version: "1.0.0"
config-version: 2

profile: carbon_mrv

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  carbon_mrv:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts